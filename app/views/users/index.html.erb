<h3>ユーザー検索結果</h3>
<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.nickname, user_path(user) %></li>
    <% end %>
  </ul>
<% else %>
  <p>該当するユーザーはいません。</p>
<% end %>