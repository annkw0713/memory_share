<h3>フォローされているユーザー</h3>
<% if @follower_users.any? %>
  <% @follower_users.each do |user| %>
    <p><%= link_to user.nickname, user_path(user.id) %></p>
  <% end %>
<% else %>
  <p>該当するユーザーはいません。</p>
<% end %>